// export const sCoinTypeToName = Object.entries(sCoinIds).reduce(
//   (acc, [coinName, coinType]) => {
//     acc[coinType] = coinName as types.string;
//     return acc;
//   },
//   {} as Record<string, types.string>
// );

// export const sCoinRawNameToName = Object.entries(sCoinIds).reduce(
//   (acc, [coinName, coinType]) => {
//     acc[coinType.split('::')[2].toLowerCase()] = coinName as types.string;
//     return acc;
//   },
//   {} as Record<string, types.string>
// );
